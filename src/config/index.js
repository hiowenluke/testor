
const me = {
	server: {
		protocol: 'http',
		host: 'localhost',
		port: 3000,
	},

	cliOptions: {
		logs: false,
		userConfigFile: null,
		waitTime: 1000, // milliseconds
	}
};

module.exports = me;

